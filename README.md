# reto-IBM

Build docker images

```sh
$ cd reverse-proxy/
$ docker build -t reverse-proxy .
```

```sh
$ cd backend/
$ docker build -t app-sumar .
```
Create and start containers with:

```sh
$ docker-compose up -d
```
